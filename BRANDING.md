# v3rya Branding Note

The user-facing product name is **v3rya**. The public document title, visible interface copy, and configured application title use this name.

The internal project directory, package slug, storage asset keys, and template snapshot may retain the legacy `nitevow` identifier. These values are technical implementation identifiers rather than public-facing branding; retaining them avoids disrupting the active project path, build configuration, and existing media URLs.
