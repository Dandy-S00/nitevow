import { Menu, MoonStar, Plus, ShieldCheck, X } from "lucide-react";
import { useEffect, useId, useState } from "react";
import { useLocation } from "wouter";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { Button } from "@/components/ui/button";

const links = [{ label: "Discover", mobileLabel: "Discover", path: "/browse", emphasis: "primary" }, { label: "Your space", mobileLabel: "Your space", path: "/profile", emphasis: "primary" }, { label: "How safety works", mobileLabel: "Safety", path: "/safety", emphasis: "secondary" }] as const;
const activePath = (location: string, path: string) => location === path || location.startsWith(`${path}/`);

export default function SiteHeader() {
  const [location, setLocation] = useLocation(); const { user, loading, isAuthenticated, logout } = useAuth(); const [menuOpen, setMenuOpen] = useState(false); const menuId = useId(); const navigate = (path: string) => { setLocation(path); setMenuOpen(false); };
  useEffect(() => { const closeOnEscape = (event: KeyboardEvent) => { if (event.key === "Escape") setMenuOpen(false); }; window.addEventListener("keydown", closeOnEscape); return () => window.removeEventListener("keydown", closeOnEscape); }, []);
  return <header className="sticky top-0 z-40 border-b border-white/8 bg-[#080d1df2] backdrop-blur-xl"><div className="container relative flex h-[76px] items-center justify-between gap-5"><button onClick={() => navigate("/")} className="group flex items-center gap-3 text-left" aria-label="v3rya home"><span className="flex h-9 w-9 items-center justify-center rounded-full border border-[#e1c687]/40 bg-[#e1c687]/10 text-[#e1c687] transition group-hover:bg-[#e1c687]/20"><MoonStar className="h-4 w-4" /></span><span><span className="block font-serif text-xl leading-none tracking-tight text-white">v3rya</span><span className="mt-1 block text-[10px] font-medium tracking-[.18em] text-slate-500 uppercase">Private connections</span></span></button><nav className="hidden items-center gap-7 md:flex" aria-label="Main navigation">{links.map((link) => { const active = activePath(location, link.path); return <button key={link.path} onClick={() => navigate(link.path)} aria-current={active ? "page" : undefined} className={link.emphasis === "secondary" ? `ml-1 border-b pb-1 text-xs transition ${active ? "border-slate-300 text-slate-100" : "border-transparent text-slate-500 hover:text-slate-300"}` : `relative border-b pb-1 text-sm transition ${active ? "border-[#e1c687] text-[#e1c687]" : "border-transparent text-slate-300 hover:border-white/25 hover:text-[#e1c687]"}`}>{link.label}</button>; })}</nav><div className="flex items-center gap-2">{!loading && isAuthenticated ? <><button onClick={() => navigate("/post")} className="hidden items-center gap-2 rounded-xl bg-[#e1c687] px-4 py-2.5 text-sm font-semibold text-[#10172c] transition hover:bg-[#f0da9f] sm:flex"><Plus className="h-4 w-4" /> Create listing</button><button onClick={() => navigate("/inbox")} className={`hidden rounded-xl border px-4 py-2.5 text-sm transition md:block ${activePath(location, "/inbox") ? "border-[#e1c687]/55 bg-[#e1c687]/10 text-[#e1c687]" : "border-white/10 text-slate-200 hover:border-[#e1c687]/50 hover:text-white"}`}>Messages</button><button onClick={logout} className="hidden text-xs text-slate-500 transition hover:text-slate-200 lg:block">Sign out</button><button onClick={() => navigate("/profile")} className={`flex h-9 w-9 items-center justify-center rounded-full border text-sm font-semibold transition ${activePath(location, "/profile") ? "border-[#e1c687] bg-[#e1c687]/15 text-[#e1c687]" : "border-white/15 bg-white/5 text-[#e1c687]"}`} aria-label="Open your profile">{(user?.name || "V").slice(0, 1).toUpperCase()}</button></> : <Button onClick={() => startLogin()} className="hidden h-10 rounded-xl bg-[#e1c687] px-4 text-sm font-semibold text-[#10172c] hover:bg-[#f0da9f] sm:inline-flex"><ShieldCheck className="mr-2 h-4 w-4" />Sign in</Button>}<button onClick={() => setMenuOpen((open) => !open)} className="flex h-10 w-10 items-center justify-center rounded-xl border border-white/10 text-slate-300 transition hover:border-[#e1c687]/45 hover:text-[#e1c687] md:hidden" aria-label={menuOpen ? "Close navigation" : "Open navigation"} aria-expanded={menuOpen} aria-controls={menuId}>{menuOpen ? <X className="h-4 w-4" /> : <Menu className="h-4 w-4" />}</button></div>{menuOpen && <div id={menuId} className="absolute inset-x-0 top-[calc(100%+1px)] border-b border-white/10 bg-[#0b1124]/98 px-5 py-5 shadow-2xl backdrop-blur-xl md:hidden" role="dialog" aria-label="Mobile navigation"><nav className="grid gap-1" aria-label="Mobile navigation">{links.map((link) => { const active = activePath(location, link.path); return <button key={link.path} onClick={() => navigate(link.path)} aria-current={active ? "page" : undefined} className={`flex min-h-12 items-center justify-between rounded-xl px-4 text-left transition ${active ? "bg-[#e1c687]/12 text-[#e1c687]" : "text-slate-200 hover:bg-white/5"}`}><span className={link.emphasis === "secondary" ? "text-sm text-slate-400" : "text-base font-medium"}>{link.mobileLabel}</span>{active && <span className="h-1.5 w-1.5 rounded-full bg-[#e1c687]" aria-hidden="true" />}</button>; })}</nav><div className="mt-4 border-t border-white/10 pt-4">{isAuthenticated ? <div className="grid grid-cols-2 gap-3"><button onClick={() => navigate("/post")} className="flex min-h-11 items-center justify-center gap-2 rounded-xl bg-[#e1c687] px-4 text-sm font-semibold text-[#10172c] hover:bg-[#f0da9f]"><Plus className="h-4 w-4" />Create listing</button><button onClick={() => navigate("/inbox")} className={`min-h-11 rounded-xl border px-4 text-sm ${activePath(location, "/inbox") ? "border-[#e1c687]/55 text-[#e1c687]" : "border-white/10 text-slate-200"}`}>Messages</button><button onClick={() => { logout(); setMenuOpen(false); }} className="col-span-2 min-h-10 text-sm text-slate-500 hover:text-slate-200">Sign out</button></div> : <Button onClick={() => { setMenuOpen(false); startLogin(); }} className="h-11 w-full rounded-xl bg-[#e1c687] text-sm font-semibold text-[#10172c] hover:bg-[#f0da9f]"><ShieldCheck className="mr-2 h-4 w-4" />Sign in</Button>}</div></div>}</div></header>;
}
